# Enable logging subsystem
CONFIG_LOG=y

# Set default log level (0=none, 1=err, 2=warn, 3=info, 4=debug)
CONFIG_LOG_DEFAULT_LEVEL=3

# Enable your preferred logging backend (UART console is common)
CONFIG_UART_CONSOLE=y
CONFIG_SERIAL=y
